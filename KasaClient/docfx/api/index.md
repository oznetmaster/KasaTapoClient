# API reference

The API reference section contains the generated .NET reference documentation for the `KasaTapoClient` namespace and related types.

Use the navigation pane to browse devices, discovery helpers, modules, models, and supporting transport or parser types where exposed.
